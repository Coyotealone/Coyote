$(function() {
    var pickerOpts = { dateFormat:"yy-mm-dd" };
    $( "#coyote_sitebundle_schedule_timetable" ).datepicker(pickerOpts);
});

$(function() {
    var pickerOpts = { dateFormat:"yy-mm-dd" };
    $( "#datestart" ).datepicker(pickerOpts);
});

$(function() {
    var pickerOpts = { dateFormat:"yy-mm-dd" };
    $( "#dateend" ).datepicker(pickerOpts);
});