{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FormOrder list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ordernumber</th>
                <th>Created_at</th>
                <th>Amount</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('formorder_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.ordernumber }}</td>
                <td>{% if entity.createdat %}{{ entity.createdat|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.amount }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('formorder_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('formorder_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('formorder_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
