{% extends 'CoyoteSiteBundle:Base:base.html.twig' %}

{% block javascripts %}
    <script src="{{ asset('bundles/coyotesite/js/jquery-1.10.2.js') }}"/></script>
    <script src="{{ asset('bundles/coyotesite/js/jquery-ui.js') }}"/></script>
    <script src="{{ asset('bundles/coyotesite/js/script_scheduleslocked.js') }}"/></script>
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('bundles/coyotesite/css/jquery-ui.css') }}" type="text/css" media="all" />
{% endblock %}

{% block nav %}

    <!--[if lt IE 9]>
        {{ include('CoyoteSiteBundle:Base:navigation_ie.html.twig') }}
    <![endif]-->
    <!--[if !IE]><!-->
        {{ include('CoyoteSiteBundle:Base:navigation.html.twig') }}
    <!--<![endif]-->

{% endblock %}

{% block content %}

	{% for flashMessage in app.session.flashbag.get('schedules_locked') %}
        {% set message = flashMessage %}
        {% if message == "schedule.flash.schedules_locked" %}
            <p id="flashmessage_valid">{{ flashMessage|trans }}</p>
        {% endif %}
    {% endfor %}

<p id="title">{{'schedule.title.schedules_locked'|trans}} </p>
<form role="form" action="{{ path('schedule_postschedules_locked') }}" method='post'>
	<p>{{ 'schedule.schedules_lock_message'|trans }}</p>
    <div class='table-responsive'>
        <table class='table' id='tab_expense'>
            <tr>
                <th class="center">{{ 'schedule.date'|trans }}</th>
            </tr>
            <td>
                <input type='text' name='date' id='date' >
            </td>
        </table>
    </div><br>
<button class='btn_red' type="submit" value="Enregistrer" id="enregistrer">{{ 'schedule.submit.update'|trans }}</button>
</form>
{% endblock %}