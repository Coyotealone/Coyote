{% extends 'CoyoteSiteBundle:Schedule:index.html.twig' %}

{% block content %}
    <div class="jumbotron">
        <p class="lead">{{ "schedule.title.overtime"|trans }}<br>
        {% if overtime %}
            {{overtime}}
        {% endif %}
        </p>
    </div>
{% endblock %}