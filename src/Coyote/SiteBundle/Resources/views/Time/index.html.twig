{% extends 'CoyoteSiteBundle::base.html.twig' %}

{% block userinfo %}

{{ 'Bienvenue'|trans }} {{ app.session.get('username') }} |
<a href="{{ path('fos_user_security_logout') }}">
    {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
</a>

{% endblock %}

{% block link %}
    
    <!--[if lt IE 9]>
        {{ include('CoyoteSiteBundle::navigation_ie.html.twig') }}
    <![endif]-->
    <!--[if !IE]><!-->
        {{ include('CoyoteSiteBundle::navigation.html.twig') }}
	<!--<![endif]-->
	
{% endblock %}

{% block stylesheets %}
   {{ include('CoyoteSiteBundle::css.html.twig') }}
{% endblock %}

{% block content %}

    <div class="jumbotron">
	        <p class="lead">{{'Choisissez votre méthode de saisie des temps.'|trans}}</p>
	        <p>
	        	<a href={{ path('coyote_time_user')}} class="btntime">{{'Saisie par heure'|trans}}</a>
	        	<a href={{ path('coyote_time_framework')}} class="btntime">{{'Saisie par jour'|trans}}</a>
		  	</p>
    </div>

{% endblock %}